<%= form_with(model: reply, url:board_comment_replies_path(params[:board_id], params[:id]), local: true) do |form| %>
  <% if reply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reply.errors.count, "error") %> prohibited this reply from being saved:</h2>

      <ul>
      <% reply.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="input-field col s10">
      <%= form.label :body %>
      <%= form.text_field :body %>
    </div>
    <div class="input-field col s2">
      <%= form.submit class:"btn waves-effect waves-light black white-text"%>
    </div>
  </div>

<!--  <div class="field">-->
    <%#= form.label :body %>
    <%#= form.text_field :body %>
<!--  </div>-->

<!--  <div class="actions">-->
    <%#= form.submit %>
<!--  </div>-->
<% end %>
